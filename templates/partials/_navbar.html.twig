<nav class="navbar navbar-expand-lg bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Navbar</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="#">{% trans %}Home{% endtrans %}</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">{% trans %}Link{% endtrans %}</a>
				</li>
			</ul>
			<ul class="navbar-nav ml-auto">
				{# {% for locale in locales %}
					{% if locale != app.request.locale %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('change_locale', { 'locale': locale }) }}"
                                <img src="{{ asset('images/'~locale~'.png') }}" alt="{{ locale }}">
                            </a>
						</li>
					{% endif %}
				{% endfor %} #}

                {% for locale in locales %}
                    {% if locale != app.request.locale %}
                        <a href="{{ path('change_locale', {'locale': locale}) }}"><img src="{{ asset('images/'~locale~'.png') }}" alt="{{ locale }}"></a>
                    {% endif %}
                {% endfor %}
			</ul>
		</div>
	</div>
</nav>
